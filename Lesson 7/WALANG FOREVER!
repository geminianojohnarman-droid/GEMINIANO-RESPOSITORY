#--------------------------------------------------------------------------------------------------------#

# Error_Coding Solo

'''
import tkinter as tk
from tkinter import messagebox


def show_error_notice():
    """
    Creates a hidden main Tkinter window and immediately calls the
    messagebox.showerror function to display an error notice on the screen.
    """
    # 1. Initialize the main Tkinter window (required for messagebox)
    root = tk.Tk()
    # 2. Hide the main window since we only want the error pop-up
    root.withdraw()

    # 3. Display the CRITICAL ERROR NOTICE on the screen
    messagebox.showerror(
        title="System Alert: Critical Error 99",
        message="An unexpected file detected have corrupted files in windows (C:) \n\nCode: 404-NOT-FOUND-666\n\nPlease restart your computer immediately."
    )

    # 4. Destroy the hidden main window after the user clicks 'OK'
    root.destroy()


# Run the function to show the error notice
if __name__ == "__main__":
    show_error_notice()
'''

#--------------------------------------------------------------------------------------------------------#
#
#
#============================================
#
#
#--------------------------------------------------------------------------------------------------------#

# Error_Coding_Multiple
